newassets - Theme CSS, JS, images and vendor scripts

Contents (css/, js/, img/, vendor/, fonts/) are used by the frontend layout.
If this folder is missing or out of date on a new deploy, copy it from the
live/production server or restore from git history (see project plan).

Do not remove asset('newassets/...') references from views.
